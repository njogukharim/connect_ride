<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph4"
    app:startDestination="@id/welcome">

    <activity
        android:id="@+id/welcome"
        android:name="com.kharim.connectride.Welcome"
        android:label="activity_welcome"
        tools:layout="@layout/activity_welcome" />
    <activity
        android:id="@+id/vehicle"
        android:name="com.kharim.connectride.vehicle.Vehicle"
        android:label="activity_vehicle"
        tools:layout="@layout/activity_vehicle" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.kharim.connectride.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/categoryView"
        android:name="com.kharim.connectride.categories.CategoryView"
        android:label="activity_category_view"
        tools:layout="@layout/activity_category_view" />
</navigation>